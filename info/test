<form action="" method="post">
                        <div class="col-lg-6">
                            {% csrf_token %}
                            {{club_form|bootstrap}}
                        </div>
                        <div class="col-lg-6">
                        {% if post_form %}
                            {% csrf_token %}
                            {{post_form|bootstrap}}
                        {% endif %}
                        </div>
                        <input type="submit" value="Submit" class="btn btn-success nomi-filter" />
                    </form>